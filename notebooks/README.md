# Notebook Export Notes

The final submission must be in PDF format with code, outputs, timestamp, VM ID, and virtual-lab screenshots.

The implemented solution files are currently in `../solutions/`:

- `Team_108_MAB.py`
- `Team_108_DP.py`

Recommended workflow in the virtual lab:

1. Open each script in VS Code or Jupyter.
2. Run the full file so the timestamp, VM ID, output tables, and figures are generated.
3. Export each part separately as PDF using the required naming format.
4. Add virtual-lab screenshots with visible timestamp before final submission.
